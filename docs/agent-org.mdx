---
title: "Agent Organization"
description: "Multi-agent architecture patterns for orchestrating collaborative AI agents"
---

Ante supports multiple patterns for organizing agents to work together. Each architecture trades off between autonomy, coordination overhead, and result quality.

## Independent

Agents work in parallel on the same problem with no interaction. An aggregator synthesizes their outputs at the end.

**Best for:** tasks where diverse independent perspectives improve quality (brainstorming, redundant verification).

```mermaid
flowchart TB
    Start([Start]) --> Par{{"Parallel"}}

    Par --> A1[Agent 1]
    Par --> A2[Agent 2]
    Par --> A3[Agent 3]

    A1 --> Sync{{"Sync"}}
    A2 --> Sync
    A3 --> Sync

    Sync --> Agg[Aggregator<br/>Synthesis]
    Agg --> End([End])

    style Par fill:#e1f5ff
    style Sync fill:#e1f5ff
    style A1 fill:#fff4e6
    style A2 fill:#fff4e6
    style A3 fill:#fff4e6
```

## Decentralized

Agents run in parallel rounds, reading each other's prior outputs and proposing refinements. After a fixed number of rounds, consensus is formed without a central coordinator.

**Best for:** debate-style reasoning, peer review, or negotiation where no single authority should dominate.

```mermaid
flowchart TB
    Start([Start]) --> Init[Initialize]
    Init --> LS{{"Loop: N Rounds"}}

    LS --> Par{{"Parallel"}}

    Par --> A1["Agent 1<br/>Read & Propose"]
    Par --> A2["Agent 2<br/>Read & Propose"]
    Par --> A3["Agent 3<br/>Read & Propose"]

    A1 --> Sync{{"Sync"}}
    A2 --> Sync
    A3 --> Sync

    Sync --> Save[Save to File]
    Save --> Update[Update Variables]
    Update --> LE{{"Loop End"}}

    LE -->|Continue| LS
    LE -->|Done| Consensus[Consensus<br/>Formation]

    Consensus --> End([End])

    style Par fill:#e1f5ff
    style Sync fill:#e1f5ff
    style A1 fill:#fff4e6
    style A2 fill:#fff4e6
    style A3 fill:#fff4e6
    style LS fill:#ffe6f0
    style LE fill:#ffe6f0
```

## Centralized Iterative

A central orchestrator decomposes the problem, dispatches sub-agents in parallel, evaluates their results, and decides whether to refine or finish.

**Best for:** complex tasks that benefit from top-down planning with quality gates (code generation with review, multi-step research).

```mermaid
flowchart TB
    Start([Start]) --> Setup[Setup:<br/>CONTINUE=true]

    Setup --> LS{{"Loop Start"}}

    LS --> Orch1["Orchestrator<br/>Decompose/Refine"]

    Orch1 --> Par{{"Parallel"}}

    Par --> S1[Sub-Agent 1]
    Par --> S2[Sub-Agent 2]
    Par --> S3[Sub-Agent 3]

    S1 --> W[Write Results]
    S2 --> W
    S3 --> W

    W --> OrchEval["Orchestrator (Evaluate):<br/>• Evaluate quality<br/>• Update CONTINUE"]

    OrchEval --> LE{{"Loop End"}}

    LE -->|Quality OK<br/>OR Max Rounds| Final[Final<br/>Synthesis]
    LE -->|Need Refine| LS

    Final --> End([End])

    style Par fill:#e1f5ff
    style S1 fill:#fff4e6
    style S2 fill:#fff4e6
    style S3 fill:#fff4e6
    style Orch1 fill:#e6f3ff
    style OrchEval fill:#ffe6e6
    style LS fill:#ffe6f0
    style LE fill:#ffe6f0
```

## Hybrid Iterative

Combines centralized orchestration with decentralized peer refinement. The orchestrator plans and dispatches agents, then agents refine each other's work in a peer round before the orchestrator evaluates.

**Best for:** high-quality collaborative output where both structured planning and peer feedback matter (collaborative writing, architecture design).

```mermaid
flowchart TB
    Start([Start]) --> Setup[Setup Files]

    Setup --> LS{{"Loop Start"}}

    LS --> Orch["Orchestrator<br/>Plan"]

    Orch --> Par1{{"Parallel"}}

    Par1 --> A1[Agent 1]
    Par1 --> A2[Agent 2]
    Par1 --> A3[Agent 3]

    A1 --> Sync1{{"Sync"}}
    A2 --> Sync1
    A3 --> Sync1

    Sync1 --> Par2{{"Parallel"}}

    Par2 --> PR1["Peer 1<br/>Refine using 2,3"]
    Par2 --> PR2["Peer 2<br/>Refine using 1,3"]
    Par2 --> PR3["Peer 3<br/>Refine using 1,2"]

    PR1 --> Sync2{{"Sync"}}
    PR2 --> Sync2
    PR3 --> Sync2

    Sync2 --> OrchEval["Orchestrator (Evaluate):<br/>• Read communications<br/>• Evaluate quality<br/>• Write control (CONTINUE/STOP)"]

    OrchEval --> LE{{"Loop End"}}

    LE -->|STOP<br/>OR Max| Final[Final<br/>Synthesis]
    LE -->|CONTINUE| LS

    Final --> End([End])

    style Orch fill:#e6f3ff
    style Par1 fill:#e1f5ff
    style Par2 fill:#e1f5ff
    style Sync1 fill:#e1f5ff
    style Sync2 fill:#e1f5ff
    style A1 fill:#fff4e6
    style A2 fill:#fff4e6
    style A3 fill:#fff4e6
    style PR1 fill:#ffe6f0
    style PR2 fill:#ffe6f0
    style PR3 fill:#ffe6f0
    style OrchEval fill:#ffe6e6
    style LS fill:#ffe6f0
    style LE fill:#ffe6f0
```

## Choosing an architecture

| Architecture | Coordination | Iteration | Use when |
|---|---|---|---|
| **Independent** | None | Single pass | You need diverse perspectives without interaction overhead |
| **Decentralized** | Peer-to-peer | Fixed rounds | Agents should self-organize without a central authority |
| **Centralized Iterative** | Orchestrator-driven | Quality-gated | You need structured decomposition with evaluation checkpoints |
| **Hybrid Iterative** | Orchestrator + peers | Quality-gated | You want both top-down planning and bottom-up peer refinement |
