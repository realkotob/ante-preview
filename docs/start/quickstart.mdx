---
title: "Quickstart"
description: "Install Ante and start using it in under a minute"
---

## Prerequisites

- An API key or subscription from at least one LLM provider (Anthropic, OpenAI, etc.) — or use [offline mode](/offline) with no API key

## Installation

<Info>Installation instructions coming soon.</Info>

## Quick examples

### Interactive session

```bash
# Launch the TUI — chat with the agent, approve tool calls, view diffs
ante
```

### Headless one-shot

```bash
# Run a task and exit
ante -p "add error handling to src/main.rs"
```

### Pipe input from stdin

```bash
# Pipe file contents for analysis
cat src/lib.rs | ante -p "review this code for bugs"
```

### Use a different provider

```bash
# Override model and provider
ante --provider openai --model gpt-4o -p "refactor this function"
```

### Skip tool approvals

```bash
# YOLO mode — auto-approve all tool calls
ante --yolo "fix all clippy warnings"
```

## What's next?

<CardGroup cols={2}>
  <Card title="TUI Guide" icon="terminal" href="/tui">
    Master the interactive terminal interface.
  </Card>
  <Card title="Headless Mode" icon="code" href="/headless">
    All CLI flags and output formats.
  </Card>
  <Card title="Offline Mode" icon="wifi-slash" href="/offline">
    Run models locally with no internet.
  </Card>
  <Card title="Skills" icon="scroll" href="/skills">
    Extend Ante with portable Agent Skills.
  </Card>
</CardGroup>
